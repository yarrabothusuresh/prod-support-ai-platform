# Docker Architecture Notes - Day 1

The local environment uses PostgreSQL 16 Alpine via `docker-compose.yml`.
Persistent data is mounted under the named volume `postgres_data`.

Credentials:
- Database: `prod_support`
- Username: `prod_support`
- Password: `prod_support`
- Host Port: `5432`
